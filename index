<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Ошибка 404 — System Message</title>
<style>
  /* Общие стили страницы */
  :root{
    --bg:#c0c0c0;          /* классический серый рабочего стола */
    --wnd:#e0e0e0;         /* цвет окна */
    --title:#000080;       /* темно-синяя панель заголовка */
    --title-text:#ffffff;
    --bevel-light:#ffffff;
    --bevel-dark:#808080;
    --text:#000000;
    --button-bg:#e0e0e0;
    --monofont: "MS Sans Serif", Tahoma, Arial, sans-serif;
  }
  html,body{
    height:100%;
    margin:0;
    background: linear-gradient(#c9d6e3, #aab4c0) fixed; /* слегка винтажный */
    font-family: var(--monofont);
    -webkit-font-smoothing: none;
    display:flex;
    align-items:center;
    justify-content:center;
    color:var(--text);
  }

  /* Рабочая область (карточка-окно) */
  .win98-window {
    width: 520px;
    background: var(--wnd);
    border: 2px solid var(--bevel-dark);
    box-shadow: 4px 4px 0 rgba(0,0,0,0.15);
    /* имитация 3D-рами */
    position: relative;
    font-size:14px;
  }
  /* Верхняя панель (заголовок) */
  .titlebar {
    background: linear-gradient(var(--title), #000060);
    color: var(--title-text);
    padding:6px 8px;
    display:flex;
    align-items:center;
    gap:8px;
    height:30px;
    box-sizing:border-box;
    border-bottom: 2px solid var(--bevel-dark);
  }
  .titlebar .icon {
    width:16px; height:16px;
    background: linear-gradient(#fff,#ddd);
    border:1px solid #666;
    display:inline-block;
    box-sizing:border-box;
  }
  .titlebar .title {
    font-weight:bold;
    font-size:13px;
    user-select:none;
  }
  .titlebar .controls {
    margin-left:auto;
    display:flex;
    gap:4px;
  }
  .btn-ctrl {
    width:16px; height:16px;
    background: linear-gradient(#d0d0d0,#b0b0b0);
    border-top:1px solid var(--bevel-light);
    border-left:1px solid var(--bevel-light);
    border-bottom:1px solid var(--bevel-dark);
    border-right:1px solid var(--bevel-dark);
    display:flex; align-items:center; justify-content:center;
    font-size:11px; color:#000;
    cursor:default;
  }

  /* Содержимое окна */
  .content {
    padding:18px;
    display:flex;
    gap:12px;
  }
  .icon-warning {
    width:48px; height:48px;
    background: #fff;
    border: 2px solid #000;
    display:flex;
    align-items:center;
    justify-content:center;
    font-weight:bold;
    font-size:28px;
    color:#c00;
    box-shadow: inset -2px -2px 0 rgba(0,0,0,0.2);
  }
  .text {
    flex:1;
    color:var(--text);
  }
  .text h1{
    margin:0 0 8px 0;
    font-size:18px;
  }
  .text p{
    margin:0 0 12px 0;
    line-height:1.3;
  }

  /* Панель кнопок */
  .actions{
    padding:10px 18px;
    border-top: 2px solid var(--bevel-dark);
    display:flex;
    justify-content:flex-end;
    gap:8px;
    background: linear-gradient(#e8e8e8,#dcdcdc);
  }

  /* Кнопка в стиле Win98 (выпуклая) */
  .win-btn {
    padding:6px 12px;
    background: var(--button-bg);
    border-top:2px solid var(--bevel-light);
    border-left:2px solid var(--bevel-light);
    border-bottom:2px solid var(--bevel-dark);
    border-right:2px solid var(--bevel-dark);
    font-weight:bold;
    cursor:pointer;
    user-select:none;
  }
  /* Нажатое состояние */
  .win-btn:active{
    border-top:2px solid var(--bevel-dark);
    border-left:2px solid var(--bevel-dark);
    border-bottom:2px solid var(--bevel-light);
    border-right:2px solid var(--bevel-light);
    transform:translateY(1px);
  }

  /* Низкий контраст и пиксельный эффект */
  .win98-window, .titlebar, .win-btn, .icon-warning {
    image-rendering: pixelated;
  }

  /* Маленький экран адаптив */
  @media (max-width:560px){
    .win98-window { width:92%; }
    .text h1 { font-size:16px; }
  }
</style>
</head>
<body>
  <div class="win98-window" role="dialog" aria-labelledby="errTitle">
    <div class="titlebar">
      <span class="icon" aria-hidden="true"></span>
      <span class="title">System Message</span>
      <div class="controls" aria-hidden="true">
        <div class="btn-ctrl">—</div>
        <div class="btn-ctrl">▢</div>
        <div class="btn-ctrl">×</div>
      </div>
    </div>

    <div class="content">
      <div class="icon-warning" aria-hidden="true">!</div>
      <div class="text">
        <h1 id="errTitle">Ошибка 404 — Страница не найдена</h1>
        <p>Сервер не отвечает. Запрашиваемый ресурс отсутствует или недоступен.</p>
        <p><em>Попробуйте вернуться на главную страницу или обновить запрос.</em></p>
      </div>
    </div>

    <div class="actions">
      <button class="win-btn" onclick="toHome()">На главную</button>
      <button class="win-btn" onclick="location.reload()">Повторить</button>
    </div>
  </div>

<script>
  // Функция "На главную" — если у тебя есть главная страница, укажи URL
  function toHome(){
    // замените '/' на реальный адрес если нужно
    window.location.href = '/';
  }

  // Опционально: имитируем короткий системный звук (будет работать в некоторых браузерах)
  // Здесь закомментировано, потому что автопроигрывание может быть заблокировано
  /*
  function beep(){
    const ctx = new (window.AudioContext || window.webkitAudioContext)();
    const o = ctx.createOscillator();
    o.type = 'square';
    o.frequency.value = 600;
    o.connect(ctx.destination);
    o.start();
    setTimeout(()=>{ o.stop(); ctx.close(); }, 120);
  }
  window.onload = () => { setTimeout(beep, 200); };
  */
</script>
</body>
</html>
